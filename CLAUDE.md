# SVIL-Tarot

저시력 친화 타로 학습·리딩 웹앱.

## 버전

`VERSION` 파일 기준. 현재 v0.1.0. 패치=소수정, 마이너=기능 마일스톤.

## 스택

Vite + React + TypeScript. LLM: Ollama `gemma4:12b`. TTS: SVIL `:8765`.

## 문서

- `docs/prd/` · `docs/architecture/` · `docs/handoff/` · `docs/reports/`
- 로컬 docs와 Vault `G:\내 드라이브\SVIL Vault\03_PRJ\SVIL-Tarot\` 이중 저장

## 히스토리 메뉴

설정 화면에 버전별 업데이트 내역을 표시한다.

## UI

SVIL 고대비 다크 토큰. Stitch mock은 `asset/stitch/` 참고. CDN 폰트 금지.
